# Disable kernel log in container (no /dev/kmsg access)
module(load="imklog" permitnonkernelfacility="off" enabled="off")

# Forward auth messages to stdout for docker logs visibility
auth,authpriv.* action(type="omfile" file="/dev/stdout" fileCreateMode="0644")
